class first {
    public static void main(String[] args) {
        int[] numbers = { 9, 4, 5, 2, 3, 6, 1, 4, 7, 2, 2 };
        int max = 0;
        for (int i = 0; i < numbers.length; i++) {
            //if (numbers[i] > max) {
                max = i;

           // }

            // if (numbers[i]%2==0){
            // System.out.println(numbers[i]);
        }
        System.out.println("EN BÜYÜK SAYI " + max);

        // System.out.println(numbers[i]);
    }

}
